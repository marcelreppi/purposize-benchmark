<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link href="/css/profile.css" rel="stylesheet" type="text/css" />
    <title>Super Fitness</title>
  </head>
  <body>
    <div class="header">
      <div class="title">Super Fitness</div>
      <div class="navbar">
        <div class="nav-item nav-title">Super Fitness!</div>
        <div class="nav-item">Home</div>
        <div class="nav-item active">Profile</div>
        <div class="nav-item">Heart Rate</div>
        <div class="nav-item">Step Logs</div>
        <div class="nav-item">Access Codes</div>
        <div class="nav-item">Purposes</div>
        <div class="nav-item">Logout</div>
      </div>
    </div>

    <div class="main-container">
      <div class="page-title">Step Log</div>

      <table>
        <tr>
          <th>User</th>
          <th>Log</th>
          <th>Date</th>
        </tr>

        <% for (let log of stepLogs) { %>
        <tr>
          <td><%= user.name %></td>
          <td>
            <% for (let logEntry of log.log) { %>
            <span><%= logEntry.date %></span>
            <span><%= logEntry.value %></span>
            <% } %>
          </td>
          <td><%= log.timestamp %></td>
        </tr>
        <% } %>
      </table>
    </div>
  </body>
</html>
